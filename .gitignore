__pycache__/
*.pyc
*.pyo

# Credentials
gcloud_credentials.json

# Logs
*logs*.txt
*.log

# Generated files / artifacts
reward_curve.png
rl_dashboard.png
unsloth_compiled_cache/
sft_output/
sft_output_100ep/
sft_on_rl_output/
checkpoint_eval_results/
sft_data/trajectories_backup/
sft_data/trajectories_augmented/

# Root-level eval/experiment JSON results (not data subdirectories)
/*.json

# IDE / Editor
.vscode/
.idea/

# Environment
.env
.python-version
.art/
.claude/
uv.lock
# CLAUDE.md is intentionally tracked — it's the project's standing instructions.

# Editor / AI agent scratch
.aider*
.repomix*
.context.md

# Utilities
kill_gpu.sh

# RL run checkpoints (large model files), but track eval/diagnostics/README
rl_runs/*/checkpoint/

# Per-run output directories — track only metadata, summaries, eval results, READMEs.
# Large transient artifacts (logs, checkpoints, raw diagnostics) stay out.
runs/*/checkpoints/
runs/*/logs/
runs/*/diagnostics/*.json
runs/*/diagnostics/*.png
!runs/*/config.json
!runs/*/metadata.jsonl
!runs/*/eval/
!runs/*/diagnostics/*.csv
!runs/README.md
!runs/archive/**

# Standalone debug artifacts at repo root
heartbeat.jsonl
deadlock_detected.jsonl

# OS
.DS_Store
vllm-build/
